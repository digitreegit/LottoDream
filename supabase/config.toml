# Supabase Edge Functions config.
# The Stripe webhook authenticates via Stripe signature (not a Supabase JWT),
# so JWT verification must be disabled for it.

[functions.stripe-webhook]
verify_jwt = false

[functions.create-checkout-session]
verify_jwt = true

[functions.create-billing-portal-session]
verify_jwt = true
